[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # VITE_API_URL = "https://your-backend-app.onrender.com" # Set this in Netlify dashboard!

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
